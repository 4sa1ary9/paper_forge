[project]
name = "paperforge-agent"
version = "0.1.0"
description = "Python + Streamlit AI paper research agent workflow."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymupdf>=1.27.2.3",
    "streamlit>=1.41.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
]

[tool.uv]
package = false
